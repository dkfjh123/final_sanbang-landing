# 산방식당 랜딩 v2 — 이어서 할 일 (2026-07-27 아침)

> 2026-07-26 밤 작업 중단 시점 기록. 이 파일만 읽으면 바로 이어갈 수 있게 씀.

---

## 0. 재개 명령 (복붙용)

```
projects/20251124_sanbangjeju-landing 이어서 하자. 20260727_이어서할일.md 읽고 시작해줘.
```

서버 띄우기 (둘 다 어제 종료함):
```bash
cd C:\Users\dkfjh\devnew\projects\20251124_sanbangjeju-landing
npm run dev                          # 수정용 dev → localhost:5173
npm run build                        # 배포 산출물 생성 (루트에 index.html + build/)
python -m http.server 4173           # 빌드본 확인 → 127.0.0.1:4173
```

---

## 1. 어제 끝낸 것

**전면 개편 완료. 배포는 안 함.** 로컬에서만 돌아가는 상태.

| 항목 | 결과 |
|---|---|
| 첫 화면 무게 | 150MB → **2.18MB** (69배 ↓) |
| 전체 스크롤 | 439MB → **4.84MB** (91배 ↓) |
| 히어로 영상 | 139.7MB/4분46초 → **671KB/8.8초** 무음 루프 |
| 사진 20종 | 639.6MB 원본 → **3.45MB** WebP |
| 줄서기 GIF 3종 | 62.4MB → **0.96MB** MP4 |
| JS | Tailwind CDN 397KB(런타임 컴파일) → **70.7KB** gzip 빌드본 |
| 404 깨진 이미지 | 해결 (에셋 전량 재생성) |
| 콘솔 오류 | 0건 (데스크톱 14컷 · 모바일 21컷 육안 검증 완료) |
| 금지 표현 | 0건 |

기술 스택: Vite + React 19 + Tailwind v4. 애니메이션 라이브러리 없음(CSS로 처리).

---

## 2. 🔴 먼저 결정해야 할 것 — 나(윤지환)가 답해야 진행됨

### (1) GA4 측정 ID ← 1순위
- **지금 이 사이트엔 분석 도구가 0개.** 방문자도 문의 수도 아무도 모름.
- 코드에 자리는 만들어둠 → `app/src/App.tsx` 상단 `const GA4_ID = "";`
- **할 일**: GA4 속성 생성 → `G-XXXXXXX` 형식 ID 받아서 넣기
- 이거 없으면 이번 개편 효과도 또 측정 불가. 같은 실수 반복.

### (2) 이메일 주소 확인
- 기존 랜딩: `dkfjh1234@gmail.com` (개인 지메일)
- 새 랜딩에 넣은 것: `contact@jejusanbang.com` (B2B 플레이북이 지정한 공식 창구)
- **확인 필요**: 이 주소로 실제 메일이 수신되는지. 안 되면 되돌리거나 새로 파야 함.

### (3) Tally 폼 확인
- 기존 폼 ID `WOOLLv`를 그대로 연결함 (`https://tally.so/r/WOOLLv`)
- **확인 필요**: 이 폼이 B2B 문의용이 맞는지. 질문 항목이 새 구조(기존 운영자 / 급식·단체)에 맞는지.

### (4) 배포 여부
- `git push` = **sanbangjeju.com에 즉시 반영**. 스테이징 없음.
- 되돌리려면 이전 커밋으로 revert (구버전은 git 히스토리에 보존됨).

---

## 3. 내일 할 일 (순서대로)

- [ ] **1. 4173에서 빌드본 눈으로 보기** → 고칠 곳 지적
- [ ] **2. 위 (1)~(3) 결정** (GA4 ID / 이메일 / Tally)
- [ ] **3. 수정 반영 → 재빌드 → 재확인**
- [ ] **4. 배포** (`git add . && git commit && git push`)
- [ ] **5. 배포 후 Cloudflare Cache Rule 추가** ← 대시보드 작업, 내가 직접 해야 함
      - 현재 `cf-cache-status: DYNAMIC` = 엣지 캐시 완전히 꺼져 있음
      - Rule: 호스트 `sanbangjeju.com` → Edge TTL 1년, Browser TTL 1년
      - 무료 플랜에서도 됨
- [ ] **6. 배포 직후 실제 사이트 무게 재측정** (`node _weigh.mjs https://sanbangjeju.com/`)

### 여유 되면
- [ ] 폰트 613KB 줄이기 — Pretendard 동적 서브셋이 613KB 씀. 실제 쓰는 글자만 자체 서브셋팅하면 ~80KB까지 가능
- [ ] OG 이미지 전용 컷 제작 (지금은 `lineup-top.webp` 재사용)
- [ ] 줄서기 영상에 원본 자막("제주점" 등)이 박혀 있고 내가 넣은 캡션과 중복 → 하나로 정리

---

## 4. 확정된 방향 (다시 논의하지 말 것)

2026-07-26 레드팀 검증 결과:

| 페르소나 | 결정 |
|---|---|
| **기존 식당 운영자** | ✅ 주력. 페이지의 80% |
| **급식·단체 B2B** | ✅ 부력. 하단 스펙 블록만 (문의 유도 X, **실사 통과**용) |
| ~~개인창업 예정자~~ | ❌ **제외**. 이유: ①이 사람이 원하는 '간판'을 가맹법상 못 줌 ②공식 사이트로 유입 안 됨(광고 필요) ③"가맹인가요?" 응대 반복 = 리스크 확대 → 나중에 SPOT 랜딩 + 메타광고로 분리 |
| ~~돈까스·제육 (민들레푸드)~~ | ❌ **제외**. 산방 제품 아님 + 민들레 대외 비공개 + 공급형태 미확정 + 이미지가 AI 생성물 → **SPOT 쪽**에 붙일 것 |

**중요한 인식 정정**: 이 랜딩은 리드 생성기가 아니라 **신뢰 검증 페이지**다.
B2B 플레이북상 실제 획득 경로는 `콜드 메일 → 15분 통화`이고, 랜딩은 메일 서명의 링크로 등장한다.
→ 목표는 "문의 폭증"이 아니라 "메일 받은 사장님이 링크 눌렀을 때 도망 안 가게".

---

## 5. ⚠️ 문구 금지선 (수정할 때 반드시 지킬 것)

출처: `workspace-100b/brands/sanbang/_context/sanbang-brand-facts.md §7` + `sanbang-b2b-email-playbook.md §0`

| 쓰면 안 되는 말 | 이유 | 대신 |
|---|---|---|
| "가맹 아님 / 가맹비 없음" **단정** | 공정위상 (주)산방에프앤비 = **등록 프랜차이즈**(2019.05~, 가맹점 6~9개) | "간판 그대로, 인테리어·로열티 부담 없이" |
| "백년가게" | 정부 지정 아님 | "100년 가는 식당을 향해" |
| "코릿 TOP10" | 공식은 '제주 TOP30' | "2016 코릿(KOREAT) 제주 선정" |
| "돼지 사골/뼈 육수" | 산방은 **멸치 베이스** | "국내산 멸치·생강을 오래 끓인 육수" |
| 가수율 45% / 다선 30년 / 육수 10년 개발 | 내부 미검증 수치 | 체감 표현으로 ("끝까지 잘 붇지 않는") |
| 매출 보장 / 무조건 성공 | 허위광고 소지 | 보장 표현 금지 |

> ※ 비교표의 "돼지뼈·사골"은 **부산식을 설명하는** 문맥이라 정상. 산방을 사골이라 하면 안 되는 것.

검수 명령:
```bash
grep -o "가맹비\|백년가게\|TOP10\|가수율\|매출 보장" build/*.js index.html
```

---

## 6. 파일 위치

```
20251124_sanbangjeju-landing/
├── app/src/App.tsx          ← 랜딩 본체 (카피 + 구조 전부 여기)
├── app/src/index.css        ← 디자인 토큰 (고위드 랭귀지 기반)
├── app/public/assets/web/   ← 최적화된 이미지·영상 25개 (5.2MB)
├── index.html               ← 빌드 산출물 (GitHub Pages가 서빙)
├── build/                   ← 빌드된 JS/CSS
├── CNAME                    ← sanbangjeju.com (건드리지 말 것)
├── _optimize.mjs            ← G드라이브 원본 → WebP 재생성
├── _shot.mjs                ← 스크린샷 검증 (fullPage 말고 뷰포트 단위)
├── _weigh.mjs               ← 전송량 실측
└── _diag.mjs                ← 스크롤 리빌 진단
```

**참조 문서**
- 전략: `projects/000_masterplan_300b/00_전략본부/14_1호유닛_산방식당_초격차패키지.md`
- 사실관계: `workspace-100b/brands/sanbang/_context/sanbang-brand-facts.md`
- 영업 문구: `workspace-100b/brands/sanbang/_context/sanbang-b2b-email-playbook.md`
- 랜딩 흐름 공식: `projects/2026billion_20260130_niplanding/논리방향구조/20260408_Sabri_Suby_세일즈페이지_17단계_공식.md`
- 디자인 랭귀지: `workspace-life/projects/20260611_디자인랭활용_고위드_레퍼런스/gowid-extract/`

---

## 7. 알아둘 함정

- **dev 서버가 stale 될 수 있음** — 어제 파일 구조 바꾼 뒤 5173이 옛 화면을 계속 서빙했다. 이상하면 서버 재시작.
- **fullPage 스크린샷 믿지 말 것** — 긴 페이지에서 섹션이 통째로 빈 것처럼 찍힌다. 실제론 멀쩡했음. `_shot.mjs`는 뷰포트 단위로 찍게 고쳐놨다.
- **빌드 산출물이 저장소 루트에 떨어진다** — Vite 경고 2개는 정상(의도한 설정). `emptyOutDir:false`라 CNAME·.git 안 날아감.
- **원본 에셋은 G드라이브** — `G:\내 드라이브\2. Areas (G-Drive)\2. Brand_assets_산방식당`. 사진 다시 뽑으려면 `_optimize.mjs`의 jobs 배열 수정.
